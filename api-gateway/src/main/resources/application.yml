spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: http://localhost:8081
          predicates:
            - Path=/users/**
        - id: ticket-service
          uri: http://localhost:8082
          predicates:
            - Path=/tickets/**
